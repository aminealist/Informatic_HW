#include<bits/stdc++.h>

#define ll long long
#define ld long double
#define openf(file) ifstream in(file)
#define fori(start, end) for(ll i = start; i < end; ++i)
using namespace std;


int main() {

}